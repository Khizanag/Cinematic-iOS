/// Stable accessibility identifiers for UI-test hooks.
/// The UI test target mirrors these literals — keep both sides in sync.
public enum AccessibilityID {
    public static let discoverList = "discover.list"
    public static let searchResults = "search.results"
    public static let favoritesList = "favorites.list"
    public static let movieDetail = "movieDetail.container"
}
