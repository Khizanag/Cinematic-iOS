/// The single namespace for every design token in the app.
/// Access tokens as `DesignSystem.Color.accent`, `DesignSystem.Spacing.md`, etc.
/// Feature code never references raw values — always route through this namespace.
public enum DesignSystem {}
